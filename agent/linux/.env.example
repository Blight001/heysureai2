SERVER_URL=http://localhost:3000
AGENT_TOKEN=
AGENT_ID=
AGENT_NAME=Linux Agent
AGENT_GROUP=
WORKSPACE_ROOT=/home/<your-username>/HeySureWorkspace
# Optional: external speech-to-text command for the ear.* tools. It must listen
# on the microphone and print one JSON object per line, e.g.
# HS_STT_CMD=vosk-transcriber --model /opt/vosk --json
HS_STT_CMD=
