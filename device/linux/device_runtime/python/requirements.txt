# HeySure 桌面端 Python 运行时依赖（Linux）。
# 由 scripts/setup-python.js 安装进 device_runtime/python/.venv，
# 供服务器下发的 runtime=python 工具调用（见 device/shared/src/runtime/python-runner.ts）。
# 这些是本机原生自动化库，无法在 CI/远程环境验证，请在目标机器上安装。
# Linux 下键鼠/截图依赖 X11 会话；窗口管理仍走 wmctrl/xdotool（系统命令）。

pyautogui>=0.9.54        # 鼠标 / 键盘 / 截图
pynput>=1.7.6            # 低层键鼠事件
pyperclip>=1.8.2         # 剪贴板（需要 xclip/xsel）
mss>=9.0.1               # 高性能截图
Pillow>=10.0.0           # 图像处理
psutil>=5.9.0            # 进程 / 系统信息
python-xlib>=0.33        # pyautogui 在 X11 下的依赖
pyttsx3>=2.90            # 文字转语音 (speech.speak，依赖 espeak/espeak-ng)
# 注意：display.box 用标准库 tkinter，无法用 pip 安装；Linux 需系统包 python3-tk
#       (sudo apt install python3-tk)。Windows 的 Python 自带 tkinter。

# 视觉 / OCR（较重，按需取消注释）：
# opencv-python>=4.9.0
