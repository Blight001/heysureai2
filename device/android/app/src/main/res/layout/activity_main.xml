<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Header: logo + status pill -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingBottom="14dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/app_name"
                android:textColor="@color/text"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="@string/app_subtitle"
                android:textColor="@color/muted"
                android:textSize="10sp" />

            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:background="@drawable/pill_bg"
                android:paddingHorizontal="12dp"
                android:paddingVertical="6dp">

                <View
                    android:id="@+id/statusDot"
                    android:layout_width="10dp"
                    android:layout_height="10dp"
                    android:layout_marginEnd="6dp"
                    android:background="@drawable/status_dot" />

                <TextView
                    android:id="@+id/statusText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/status_disconnected"
                    android:textColor="@color/text"
                    android:textSize="12sp" />
            </LinearLayout>
        </LinearLayout>

        <!-- Card: account / login -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/card_bg"
            android:padding="14dp"
            android:layout_marginBottom="12dp">

            <TextView style="@style/CardTitle" android:text="@string/card_account" />

            <EditText
                android:id="@+id/serverUrlInput"
                style="@style/Field"
                android:hint="@string/server_url_hint"
                android:inputType="textUri" />

            <EditText
                android:id="@+id/accountInput"
                style="@style/Field"
                android:hint="@string/account_hint"
                android:inputType="text" />

            <EditText
                android:id="@+id/passwordInput"
                style="@style/Field"
                android:hint="@string/password_hint"
                android:inputType="textPassword" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/loginButton"
                style="@style/BtnPrimary"
                android:text="@string/login" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/logoutButton"
                style="@style/BtnSecondary"
                android:text="@string/logout" />

            <TextView style="@style/Hint" android:text="@string/hint_account_card" />
        </LinearLayout>

        <!-- Card: permissions -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/card_bg"
            android:padding="14dp"
            android:layout_marginBottom="12dp">

            <TextView style="@style/CardTitle" android:text="@string/card_permissions" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/accessibilityButton"
                style="@style/BtnSecondary"
                android:text="@string/enable_accessibility" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/captureButton"
                style="@style/BtnSecondary"
                android:text="@string/grant_capture" />

            <TextView style="@style/Hint" android:text="@string/hint_permissions" />
        </LinearLayout>

        <!-- Card: power / keep-awake -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/card_bg"
            android:padding="14dp"
            android:layout_marginBottom="12dp">

            <TextView style="@style/CardTitle" android:text="@string/card_power" />

            <com.google.android.material.switchmaterial.SwitchMaterial
                android:id="@+id/keepAwakeSwitch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="4dp"
                android:text="@string/keep_awake"
                android:textColor="@color/text"
                android:textSize="13sp" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/batteryButton"
                style="@style/BtnSecondary"
                android:text="@string/battery_exempt" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/stopButton"
                style="@style/BtnSecondary"
                android:text="@string/stop" />

            <TextView style="@style/Hint" android:text="@string/hint_power" />
        </LinearLayout>

        <!-- Card: log -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/card_bg"
            android:padding="14dp">

            <TextView style="@style/CardTitle" android:text="@string/card_log" />

            <TextView
                android:id="@+id/logText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="monospace"
                android:textColor="@color/muted"
                android:textSize="11sp" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>
